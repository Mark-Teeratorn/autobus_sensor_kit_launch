/**:
  ros__parameters:
    min_x: -0.22    # rear_overhang
    min_y: -0.38    # half of (wheel_tread + overhangs)
    min_z: -1.6     # full vehicle height downward
    max_x: 1.25     # front_overhang + wheel_base
    max_y: 0.38     # half of (wheel_tread + overhangs)
    max_z: 0.1      # just above lidar mount
    negative: true
    processing_time_threshold_sec: 0.01