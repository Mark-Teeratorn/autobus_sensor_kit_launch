<launch>
  <arg name="launch_driver" default="true"/>

  <group>
    <push-ros-namespace namespace="imu"/>

    <!-- IMU data comes from the shared GNSS mavros instance (gnss.launch.xml).
         Do NOT launch a second mavros_node here — that causes serial port conflicts
         and duplicate nodes fighting over FCU communication. -->
    <arg name="imu_raw_name" default="/sensing/gnss/mavros_node/data_raw"/>
    <arg name="imu_corrector_param_file" default="$(find-pkg-share individual_params)/config/$(var vehicle_id)/autobus_sensor_kit/imu_corrector.param.yaml"/>
    <include file="$(find-pkg-share autoware_imu_corrector)/launch/imu_corrector.launch.xml">
      <arg name="input_topic" value="$(var imu_raw_name)"/>
      <arg name="output_topic" value="imu_data"/>
      <arg name="param_file" value="$(var imu_corrector_param_file)"/>
    </include>

    <include file="$(find-pkg-share autoware_imu_corrector)/launch/gyro_bias_estimator.launch.xml">
      <arg name="input_imu_raw" value="$(var imu_raw_name)"/>
      <arg name="input_odom" value="/localization/kinematic_state"/>
      <arg name="imu_corrector_param_file" value="$(var imu_corrector_param_file)"/>
    </include>
  </group>

  <!-- NOTE: mavros_node is intentionally NOT launched here.
       A single mavros instance is shared between IMU and GNSS,
       launched by gnss.launch.xml at /sensing/gnss/mavros_node -->

</launch>
